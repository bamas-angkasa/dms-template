export function MiniLineChart({data}:{data:number[]}){const w=300,h=92,max=Math.max(...data),min=Math.min(...data);const pts=data.map((v,i)=>`${i/(data.length-1)*w},${h-(v-min)/(max-min||1)*(h-18)-9}`).join(" ");return <div className="line-chart"><svg viewBox={`0 0 ${w} ${h}`} role="img" aria-label="Grafik tren penjualan"><defs><linearGradient id="chart-fill" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stopColor="#0057FF" stopOpacity=".25"/><stop offset="1" stopColor="#0057FF" stopOpacity="0"/></linearGradient></defs><path d={`M ${pts} L ${w},${h} L 0,${h} Z`} fill="url(#chart-fill)"/><polyline points={pts} fill="none" stroke="#0057FF" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round"/></svg><div className="chart-labels">{["Sen","Sel","Rab","Kam","Jum","Sab","Min"].map(d=><span key={d}>{d}</span>)}</div></div>}
